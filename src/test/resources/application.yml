spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:bizcart_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

bizcart:
  auth:
    jwt:
      secret: test-only-0123456789abcdef0123456789abcdef
    token-hash:
      secret: test-token-hash-0123456789abcdef0123456789abcdef
